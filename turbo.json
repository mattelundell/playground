{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "check": {},
    "build": {
      "dependsOn": ["check"],
      "outputs": [".sst"]
    },
    "deploy": {
      "dependsOn": ["build", "check"]
    }
  }
}
